# Frontend (Vite) — safe to expose, these are public keys.
VITE_SUPABASE_URL=https://kmxehgjftoashzhmfjhr.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Build-time only: base path for GitHub Pages (e.g. /style-snap-ai/).
# Leave unset for local dev (defaults to /).
# BASE_PATH=/style-snap-ai/
